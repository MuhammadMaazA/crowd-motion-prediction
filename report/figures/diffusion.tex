\begin{tikzpicture}[
  node distance=0.38cm,
  %% Full-width styles (encoder / input)
  pGrayW/.style={rectangle, rounded corners=2pt,
    minimum width=5.3cm, minimum height=0.72cm,
    align=center, text width=5.1cm, inner sep=3pt,
    fill={rgb,255:red,247;green,250;blue,252},
    draw={rgb,255:red,74;green,85;blue,104},
    line width=0.4pt, font=\scriptsize},
  pTealW/.style={rectangle, rounded corners=2pt,
    minimum width=5.3cm, minimum height=0.78cm,
    align=center, text width=5.1cm, inner sep=4pt,
    fill={rgb,255:red,237;green,242;blue,247},
    draw={rgb,255:red,44;green,62;blue,80},
    line width=0.4pt, font=\scriptsize},
  %% Narrow branch styles (3.0 cm)
  pCoralN/.style={rectangle, rounded corners=2pt,
    minimum width=3.0cm, minimum height=0.78cm,
    align=center, text width=2.8cm, inner sep=3pt,
    fill={rgb,255:red,255;green,245;blue,245},
    draw={rgb,255:red,153;green,61;blue,30},
    line width=0.4pt, font=\scriptsize},
  pGrayN/.style={rectangle, rounded corners=2pt,
    minimum width=3.0cm, minimum height=0.72cm,
    align=center, text width=2.8cm, inner sep=3pt,
    fill={rgb,255:red,247;green,250;blue,252},
    draw={rgb,255:red,74;green,85;blue,104},
    line width=0.4pt, font=\scriptsize},
  pPurpN/.style={rectangle, rounded corners=2pt,
    minimum width=3.0cm, minimum height=0.78cm,
    align=center, text width=2.8cm, inner sep=3pt,
    fill={rgb,255:red,237;green,233;blue,254},
    draw={rgb,255:red,83;green,74;blue,183},
    line width=0.4pt, font=\scriptsize},
  pFinalN/.style={rectangle, rounded corners=2pt,
    minimum width=3.0cm, minimum height=0.78cm,
    align=center, text width=2.8cm, inner sep=3pt,
    fill={rgb,255:red,44;green,62;blue,80},
    draw={rgb,255:red,44;green,62;blue,80},
    text=white, line width=0.4pt, font=\scriptsize\bfseries},
  parr/.style={-{Stealth[length=3pt,width=2.5pt]},
    line width=0.45pt,
    color={rgb,255:red,74;green,85;blue,104}},
  darr/.style={-{Stealth[length=3pt,width=2.5pt]},
    line width=0.40pt, dashed,
    color={rgb,255:red,74;green,85;blue,104}},
]

  %% ── Input ───────────────────────────────────────────────────
  \node[pGrayW] (inp) {%
    {\bfseries Trajectory} $X$ $+$ {\bfseries Neighbours} $X^n$\\[1pt]
    {\tiny obs $=8$ steps, 2-D each}};

  %% ── Shared conditioning encoder ─────────────────────────────
  \node[pTealW, below=0.38cm of inp] (enc) {%
    {\bfseries Shared Conditioning Encoder}\\[-2pt]
    {\tiny 2-layer pre-LN Transformer;\;CLS token $+$ 48 trajectory tokens}};

  %% ── Memory label ────────────────────────────────────────────
  \node[font=\tiny\itshape,
    text={rgb,255:red,74;green,85;blue,104},
    below=0.10cm of enc] (memlbl) {Memory $\mathbf{c}$ (49 tokens)};

  %% ── Left branch: Gaussian head ──────────────────────────────
  \node[pCoralN, below=0.55cm of enc, xshift=-1.75cm] (ghead) {%
    {\bfseries Gaussian MLP head}\\[-2pt]
    {\tiny CLS token $\to$ Linear $\to 5T$ params}};
  \node[pFinalN, below=0.38cm of ghead] (fastout) {%
    $\hat{y}$\;(fast, deterministic)};

  %% ── Right branch: denoiser ───────────────────────────────────
  \node[pGrayN, below=0.55cm of enc, xshift=+1.75cm] (noisein) {%
    {\bfseries Noisy} $\mathbf{x}_t$, {\bfseries time} $t$\\[1pt]
    {\tiny $T{=}100$ DDPM steps}};
  \node[pPurpN, below=0.38cm of noisein] (den) {%
    {\bfseries Denoiser} $\varepsilon_\theta$\\[-2pt]
    {\tiny Transformer;\;cross-attn to $\mathbf{c}$}};
  \node[pFinalN, below=0.38cm of den] (ddimout) {%
    DDIM-50 ${\to}K{=}20$ samples};

  %% ── Branch labels ───────────────────────────────────────────
  \node[font=\tiny\bfseries,
    text={rgb,255:red,153;green,61;blue,30},
    above=0.04cm of ghead] {FAST INFERENCE};
  \node[font=\tiny\bfseries,
    text={rgb,255:red,43;green,108;blue,176},
    above=0.04cm of noisein] {STOCHASTIC};

  %% ── Arrows ───────────────────────────────────────────────────
  \draw[parr] (inp.south) -- (enc.north);

  %% encoder → two branches
  \draw[parr] (enc.south) -- ++(0,-0.18)
    -| (ghead.north);
  \draw[parr] (enc.south) -- ++(0,-0.18)
    -| (noisein.north);

  %% left branch
  \draw[parr] (ghead.south) -- (fastout.north);

  %% right branch
  \foreach \f/\t in {noisein/den, den/ddimout}
    \draw[parr] (\f.south) -- (\t.north);

\end{tikzpicture}
