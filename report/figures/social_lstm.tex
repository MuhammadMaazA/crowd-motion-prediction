\begin{tikzpicture}[
  node distance=0.38cm,
  pGray/.style={rectangle, rounded corners=2pt,
    minimum width=2.4cm, minimum height=0.72cm,
    align=center, text width=2.2cm, inner sep=3pt,
    fill={rgb,255:red,247;green,250;blue,252},
    draw={rgb,255:red,74;green,85;blue,104},
    line width=0.4pt, font=\scriptsize},
  pTeal/.style={rectangle, rounded corners=2pt,
    minimum width=5.3cm, minimum height=0.78cm,
    align=center, text width=5.1cm, inner sep=4pt,
    fill={rgb,255:red,237;green,242;blue,247},
    draw={rgb,255:red,44;green,62;blue,80},
    line width=0.4pt, font=\scriptsize},
  pPurp/.style={rectangle, rounded corners=2pt,
    minimum width=5.3cm, minimum height=0.78cm,
    align=center, text width=5.1cm, inner sep=4pt,
    fill={rgb,255:red,237;green,233;blue,254},
    draw={rgb,255:red,83;green,74;blue,183},
    line width=0.4pt, font=\scriptsize},
  pCoral/.style={rectangle, rounded corners=2pt,
    minimum width=5.3cm, minimum height=0.78cm,
    align=center, text width=5.1cm, inner sep=4pt,
    fill={rgb,255:red,255;green,245;blue,245},
    draw={rgb,255:red,153;green,61;blue,30},
    line width=0.4pt, font=\scriptsize},
  pFinal/.style={rectangle, rounded corners=2pt,
    minimum width=5.3cm, minimum height=0.78cm,
    align=center, text width=5.1cm, inner sep=4pt,
    fill={rgb,255:red,44;green,62;blue,80},
    draw={rgb,255:red,44;green,62;blue,80},
    text=white, line width=0.4pt, font=\scriptsize\bfseries},
  parr/.style={-{Stealth[length=3pt,width=2.5pt]},
    line width=0.45pt,
    color={rgb,255:red,74;green,85;blue,104}},
]

  %% ── Input nodes ───────────────────────────────────────────
  \node[pGray] (xtraj) {%
    {\bfseries Trajectory} $X$\\[1pt]
    {\tiny obs $=8$ steps, 2-D}};
  \node[pGray, right=0.5cm of xtraj] (xnbr) {%
    {\bfseries Neighbours} $X^n$\\[1pt]
    {\tiny ${\leq}5$, within 2\,m}};

  %% SLSTM+V annotation
  \node[font=\tiny\itshape,
    text={rgb,255:red,83;green,74;blue,183},
    above=0.05cm of xtraj] {%
    ${\oplus}\,v_t{=}p_t{-}p_{t-1}$ \;(SLSTM+V)};

  %% ── Social Pooling ─────────────────────────────────────────
  \node[pTeal, below=0.72cm of xtraj, xshift=1.45cm] (pool) {%
    {\bfseries Social Pooling}\\[-2pt]
    {\tiny max-pool neighbour hidden states,\;\;$r{=}2$\,m}};

  %% ── LSTM Encoder ───────────────────────────────────────────
  \node[pPurp, below=0.38cm of pool] (enc) {%
    {\bfseries LSTM Encoder}\\[-2pt]
    {\tiny hidden\,$=128$,\;\;embed\,$=64$;\;\;${\times}8$ steps}};

  %% ── LSTM Decoder ───────────────────────────────────────────
  \node[pPurp, below=0.38cm of enc] (dec) {%
    {\bfseries LSTM Decoder}\\[-2pt]
    {\tiny conditioned on social context;\;\;${\times}12$ steps}};

  %% ── Gaussian Head ──────────────────────────────────────────
  \node[pCoral, below=0.38cm of dec] (head) {%
    {\bfseries Gaussian Head}\\[-2pt]
    {\tiny Linear$(128{\to}5)$ per predicted timestep}};

  %% ── Output ─────────────────────────────────────────────────
  \node[pFinal, below=0.38cm of head] (out) {%
    $(\mu_x,\;\mu_y,\;\sigma_x,\;\sigma_y,\;\rho)$ per step};

  %% ── Arrows: inputs → pooling ────────────────────────────────
  \draw[parr] (xtraj.south) -- ++(0,-0.28)
    -| ([xshift=-0.8cm]pool.north);
  \draw[parr] (xnbr.south) -- ++(0,-0.28)
    -| ([xshift=+0.8cm]pool.north);

  %% ── Pipeline arrows ─────────────────────────────────────────
  \foreach \f/\t in {pool/enc, enc/dec, dec/head, head/out}
    \draw[parr] (\f.south) -- (\t.north);

\end{tikzpicture}
